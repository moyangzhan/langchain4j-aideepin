version: '3'

services:
  aideepin-user-web:
    container_name: aideepin-user-web
    build:
      context: ../
      dockerfile: Dockerfile
    ports:
      - '80:80'
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
