spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://117.62.236:5432/aideepin?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8&tinyInt1isBit=false&allowMultiQueries=true
    username: postgres
    password: 
    hikari:
      connection-timeout: 10000
      validation-timeout: 3000
      idle-timeout: 60000
      login-timeout: 5
      max-lifetime: 60000
      maximum-pool-size: 10
      minimum-idle: 5
      read-only: false
  data:
    redis:
      host: 101.89.148
      port: 6379
      password: 
      database: 11
      lettuce:
        pool:
          #连接池最大连接数
          max-active: 20
          #连接池最大阻塞等待时间
          max-wait: -1
          #连接池中的最大空闲连接
          max-idle: 5
          #连接池中的最小空闲连接
          min-idle: 1

logging:
  file:
    path: D:/data/aideepin/logs
  level:
    com.baomidou.mybatisplus: DEBUG

adi:
  frontend-url: http://localhost:1002
  backend-url: http://localhost:1002/api
  proxy:
    enable: false
    host: 127.0.0.1
    http-port: 1087
  # embedding model可选值如下（见adi_ai_model中的embedding类型的数据）：
  # local:all-minilm-l6-v2 (default)
  # openai:text-embedding-3-small、openai:text-embedding-3-large
  # dashscope:text-embedding-v1、dashscope:text-embedding-v3
  # 注意：由于不同向量模型不同维度参数生成的向量特征空间不一致，无法兼容存储于一张表并进行混合搜索，建议项目初期确定好向量模型及其维度，后期如有修改，需重新生成向量数据
  embedding-model: local:all-minilm-l6-v2
  # vector-database: pgvector、neo4j
  vector-database: neo4j
  # graph-database: apache-age、neo4j
  graph-database: neo4j
  datasource:
    neo4j:
      host: 117.62.236
      port: 7687
      username: neo4j
      password: 
      database: neo4j

local:
  files: /Users/lijun/Downloads/ruoyi/aideepin/files/
  images: /Users/lijun/Downloads/ruoyi/aideepin/images/
  watermark-images: /Users/lijun/Downloads/ruoyi/aideepin/mark-images/
  thumbnails: /Users/lijun/Downloads/ruoyi/aideepin/thumbnails/
  watermark-thumbnails: /Users/lijun/Downloads/ruoyi/aideepin/watermark-thumbnails/
  tmp-images: /Users/lijun/Downloads/ruoyi/aideepin/tmp_images/
  chat-memory: /Users/lijun/Downloads/ruoyi/aideepin/chat_memory/
