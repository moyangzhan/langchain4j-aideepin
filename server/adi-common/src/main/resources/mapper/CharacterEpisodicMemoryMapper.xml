<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.moyz.adi.common.mapper.CharacterEpisodicMemoryMapper">

    <select id="listRecent" resultType="com.moyz.adi.common.entity.CharacterEpisodicMemory">
        select *
        from adi_character_episodic_memory
        where character_id = #{characterId}
          and user_id = #{userId}
          and is_deleted = false
          and is_active = true
        order by created_at desc
        limit #{limit}
    </select>

    <select id="listByEmbeddingIds" resultType="com.moyz.adi.common.entity.CharacterEpisodicMemory">
        select *
        from adi_character_episodic_memory
        where is_deleted = false
        and embedding_id in
        <foreach collection="embeddingIds" open="(" separator="," close=")" item="id">
            #{id}
        </foreach>
    </select>

    <select id="countBySourceMsgId" resultType="java.lang.Integer">
        select count(1)
        from adi_character_episodic_memory
        where source_msg_id = #{sourceMsgId}
          and is_deleted = false
    </select>

</mapper>